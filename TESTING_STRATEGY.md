# 🧪 Testing Strategy - استراتيجية الاختبارات\n\n## المرحلة 1: الاختبارات الأساسية\n\n### Services Tests\n\n```dart\n// test/services/api_client_test.dart\n\ngroup('ApiClient', () {\n  late ApiClient apiClient;\n  late MockHttpClient mockHttpClient;\n\n  setUp(() {\n    mockHttpClient = MockHttpClient();\n    apiClient = ApiClient();\n  });\n\n  group('GET requests', () {\n    test('successful request returns data', () async {\n      // Arrange\n      final response = {'id': '1', 'name': 'Test'};\n      when(mockHttpClient.get(any))\n          .thenAnswer((_) async => http.Response(jsonEncode(response), 200));\n\n      // Act\n      final result = await apiClient.get('/test');\n\n      // Assert\n      expect(result, response);\n      verify(mockHttpClient.get(any)).called(1);\n    });\n\n    test('failed request throws exception', () async {\n      // Arrange\n      when(mockHttpClient.get(any))\n          .thenAnswer((_) async => http.Response('Error', 404));\n\n      // Act & Assert\n      expect(\n        () => apiClient.get('/test'),\n        throwsException,\n      );\n    });\n  });\n\n  group('POST requests', () {\n    test('post with valid data succeeds', () async {\n      // Test implementation\n    });\n  });\n});\n```\n\n### Models Tests\n\n```dart\n// test/models/app_user_test.dart\n\ngroup('AppUser', () {\n  group('can() permission check', () {\n    test('returns true for existing permission', () {\n      final user = AppUser(\n        id: '1',\n        name: 'Test User',\n        email: 'test@example.com',\n        roles: ['admin'],\n        permissions: ['view_dashboard', 'edit_projects'],\n      );\n\n      expect(user.can('view_dashboard'), true);\n    });\n\n    test('returns false for missing permission', () {\n      final user = AppUser(\n        id: '1',\n        name: 'Test User',\n        email: 'test@example.com',\n        roles: ['user'],\n        permissions: [],\n      );\n\n      expect(user.can('admin_access'), false);\n    });\n  });\n\n  group('serialization', () {\n    test('fromJson creates correct object', () {\n      final json = {\n        'id': '1',\n        'name': 'Test',\n        'email': 'test@example.com',\n        'roles': ['admin'],\n        'permissions': ['view'],\n      };\n\n      final user = AppUser.fromJson(json);\n\n      expect(user.id, '1');\n      expect(user.name, 'Test');\n    });\n\n    test('toJson preserves all fields', () {\n      final user = AppUser(\n        id: '1',\n        name: 'Test',\n        email: 'test@example.com',\n        roles: ['admin'],\n        permissions: ['view'],\n      );\n\n      final json = user.toJson();\n\n      expect(json['id'], '1');\n      expect(json['name'], 'Test');\n    });\n  });\n});\n```\n\n### Utils Tests\n\n```dart\n// test/utils/layout_breakpoints_test.dart\n\ngroup('LayoutBreakpoints', () {\n  test('isMobile returns true for width < 600', () {\n    expect(LayoutBreakpoints.isMobile(500), true);\n    expect(LayoutBreakpoints.isMobile(600), false);\n  });\n\n  test('isTablet returns true for width 600-900', () {\n    expect(LayoutBreakpoints.isTablet(600), true);\n    expect(LayoutBreakpoints.isTablet(750), true);\n    expect(LayoutBreakpoints.isTablet(900), false);\n  });\n\n  test('isDesktop returns true for width >= 900', () {\n    expect(LayoutBreakpoints.isDesktop(900), true);\n    expect(LayoutBreakpoints.isDesktop(1200), true);\n  });\n});\n```\n\n---\n\n## المرحلة 2: Integration Tests\n\n```dart\n// test/integration_test.dart\n\nvoid main() {\n  group('End-to-End Tests', () {\n    testWidgets('User login flow', (WidgetTester tester) async {\n      // Launch app\n      await tester.pumpWidget(const LaunchQualityApp());\n\n      // Find login form\n      expect(find.byType(LoginScreen), findsOneWidget);\n\n      // Enter credentials\n      await tester.enterText(\n        find.byType(TextField).first,\n        'Najjar',\n      );\n      await tester.enterText(\n        find.byType(TextField).last,\n        'Najjar2026',\n      );\n\n      // Tap login button\n      await tester.tap(find.byType(ElevatedButton));\n      await tester.pumpAndSettle();\n\n      // Verify dashboard appears\n      expect(find.byType(StaffShell), findsOneWidget);\n    });\n  });\n}\n```\n\n---\n\n## معايير النجاح:\n\n✅ جميع الاختبارات تمرّ\n✅ تغطية ≥ 70%\n✅ لا توجد أخطاء في flutter analyze\n✅ performance > 2000ms\n"